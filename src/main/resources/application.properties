# Active profile
spring.profiles.active=@spring.profiles.active@

# Server configuration
server.port=${SERVER_PORT:8080}

# Database Configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:dpg-d47c58pr0fns73fbrlb0-a.oregon-postgres.render.com}:${DB_PORT:5432}/${DB_NAME:breaddb}
spring.datasource.username=${DB_USERNAME:postgrews}
spring.datasource.password=${DB_PASSWORD:cbsurawvO5pbN4Xsch58UGf48ZAAac3J}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# CORS Configuration
app.cors.allowed-origins=${ALLOWED_ORIGINS:http://myrza-nan.vercel.app}

# H2 Console (for development)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Logging
logging.level.com.example.demo=DEBUG